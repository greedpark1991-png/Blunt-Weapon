# V0.2.5c PATCH NOTES

## 업데이트 성격
V0.2.5a를 기준으로 한 공간 문법 재정비 · 충돌/레이어/동선 긴급 핫픽스입니다.
새 콘텐츠를 추가하기보다 실제로 지나갈 수 있는 대장간을 만드는 데 집중했습니다.

## 1. Depth Sorting 전면 정리
- 캐릭터의 발 위치 `y`를 actor baseY로 사용.
- 가구는 시각 중심이 아니라 floor footprint bottom을 baseY로 사용.
- 1층과 2층 모두 같은 규칙 적용.
- 화로, 모루, 담금질통, 큰 작업대, 숫돌, 침대, 테이블 등 앞/뒤 관계 정상화.
- ㄱ자 카운터를 상단 판/세로 다리로 분리하여 각 부분의 depth를 별도로 계산.

## 2. Collision footprint / Interaction point 분리
- `SHOP_COLLIDERS`, `LOFT_COLLIDERS`: 물체의 실제 바닥 점유 면적.
- `INTERACTIONS`, `LOFT_INTERACTIONS`: 사용할 때 사람이 서는 위치 및 반경.
- 이동 통로 한가운데에 interaction point가 놓이지 않도록 분리.
- 상호작용 선택 시 각 오브젝트의 실제 반경 내부 후보만 비교.

## 3. 빗자루 상호작용 수정
- 빗자루 pickup radius를 작은 전용 영역으로 제한.
- 숫돌/카운터 중앙에서 빗자루 E가 뜨지 않도록 수정.
- 빗자루는 실제 좌하단 빗자루 위치 근처에서만 우선 선택됨.

## 4. 북쪽 벽 열 작업 라인
- 장작/석탄 → 화로 → 모루 → 담금질통을 북쪽 벽 바로 아래 한 라인으로 재배치.
- 설비 스프라이트는 벽/바닥 경계에 붙어 보이지만 collision은 실제 floor footprint만 사용.
- 작업 공정 사이 이동 거리를 짧고 읽기 쉽게 유지.

## 5. 벽 장식 높이 재조정
- 망치 걸이와 도구 선반을 창문과 같은 벽 레이어 높이로 이동.
- 벽 장식과 바닥 가구의 시각 문법 분리.

## 6. 숫돌 / 카운터 병목 해소
- 숫돌을 중앙 작업 구역에 유지하되 카운터와 충분한 수평 간격 확보.
- 두 collider가 겹치지 않음.
- grind interaction → counter interaction 경로가 실제 pathfinding으로 연결되는지 테스트 추가.

## 7. ㄱ자 판매 카운터 충돌 수정
- 카운터 상단 판과 우측 세로 다리를 별도 footprint로 분리.
- 직원은 왼쪽/안쪽의 하나의 접근 통로를 사용.
- 카운터 판자/모서리 안으로 반쯤 파고드는 이동 차단.
- 손님은 카운터 공용면에서 대기하고 직원 공간으로 들어가지 않음.

## 8. 재료 창고 / 문 / 표지판
- 재료 창고를 좌하단 바닥 코너의 무거운 보관물처럼 재배치.
- 하단 통로를 막는 영구 라벨 제거, 근접 E 프롬프트로 확인.
- OPEN/CLOSED는 HUD가 주 정보이며 문 옆 표지판은 작게 축소.
- 문틀/문짝/손잡이를 읽을 수 있는 단순 출입문 형태 유지.

## 9. 상인 문 진입
- 상인 시작 위치를 문 바깥으로 유지.
- 문을 통과해 CUSTOMER_ZONE의 대기 위치까지 pathfinding 이동.
- 상인/문/표지판/카운터가 한곳에서 서로 끼지 않게 분리.

## 10. 측면 계단과 층 전환
- 1층 좌측 벽면 측면 목조 계단 유지.
- 어두운 벽 포켓/기둥으로 구조감 보강.
- 계단 interaction point와 층 전환 spawn point 분리.
- 2층 도착 위치를 랜딩 interaction point에서 충분히 떨어진 안전 위치로 추가 보정.
- 0.35초 재진입 쿨다운 유지.

## 11. 2층 생활공간 재정리
- 침대 2개 중앙 병렬 배치 제거.
- 형 침대: 좌측 벽 세로 배치.
- 동생 침대: 우측 벽 세로 배치.
- 개인 궤짝/장화/컵/책/정돈된 소품으로 각자 영역 구분.
- 창문 아래 공용 테이블, 중앙 러그, 수납장 생활 흔적 유지.
- 침대/테이블/궤짝/계단에 별도 floor footprint 적용.
- 2층에서도 발 위치 기반 depth sorting 사용.

## 12. 라벨 정책 간소화
- 빗자루/문/영업표지/재료창고/카운터의 영구 라벨 제거.
- 근접 상호작용바가 실제 대상 이름을 표시.
- 주요 작업 설비/진열/계단만 영구 라벨 유지.

## 13. 경고 아이콘 개선
- 대기 중인 일반 이벤트 손님의 작은 bare `!` 제거.
- 16px 픽셀 플레이트 + 어두운 외곽선 + 노랑/주황 내부 + 포인터로 변경.
- 멀리서도 응대할 NPC임을 확인 가능.

## 14. 작업 안내 연동
- 현재 목표 1개만 강조하는 기존 glow 유지/정리.
- 제작 중 현재 station, 손님 응대 counter, 청소 broom, 취침 stairs/bed 등에 연결.
- 좁은 화면 compact tracker도 유지.

## 15. Pathfinding 안정화
- diagonal corner cutting 금지.
- 막힌 경로에서 일정 시간 정체 시 재탐색.
- 시작점이 충돌 영역에 들어간 경우 nearest walkable로 복구.
- 새 레이아웃의 모든 주요 interaction point가 walkable인지 자동 테스트.

## 저장
- saveVersion 4 유지.
- V0.2.4 V3 → V4 migration 유지.
- 기존 V4 세이브 호환 유지.


## V0.2.5c 추가 반영 사항

사용자가 QA 스크린샷 위에 직접 정리한 원하는 위치를 기준으로 1층/2층 배치를 한 번 더 조정했다.

### 1층
- 숫돌을 작업대 왼쪽으로 이동해 중앙~우측 통로를 더 넓게 확보
- 판매 카운터를 우하단 쪽으로 재배치하고 플레이어 접근 동선을 정돈
- 빗자루를 하단 중앙으로 고정해 다른 상호작용을 덜 가리도록 수정
- 저장/재료 창고를 좌하단 서비스 구역으로 정리

### 2층
- 좌우 벽 쪽 침대 위치와 개인 소품 위치를 사용자 스케치에 맞게 재정렬
- 공용 테이블을 창문 아래 중앙 축으로 재조정
- 우하단 계단 출구를 더 명확한 구조로 유지

### 검증
- 모든 자동 테스트 통과
- 상호작용 포인트 walkable 상태 재검증
- 계단 이동, 제작 동선, 손님 응대 동선 유지 확인
