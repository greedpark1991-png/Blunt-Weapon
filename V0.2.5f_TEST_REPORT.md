# V0.2.5f TEST REPORT

## 결과 요약
최종 자동 테스트, JavaScript/ESM 구문 검사, Production build, 정적 Preview HTTP 응답, BGM 파일 무결성 검사를 수행했습니다.

## npm test
통과 항목:

- systems/collision/pathfinding
- UX/save/keyboard/audio
- scene integration/early sleep/renown
- layout/stair/merchant/tracker
- spatial grammar/depth/interaction
- interaction priority
- assistant positioning/delivery/loft furniture
- storage list/scroll/display separation
- message queue priority/stack
- door reservation/entry queue
- compound counter group/highlight

## Interaction Resolver
PASS

검증:
- 직접 철검 제작의 연마 단계에서 숫돌과 형제가 동시에 근접해도 숫돌 선택
- 현재 제작 대상은 일반 NPC보다 우선
- 창고와 형제가 같은 위치권에 있어도 창고 우선
- 동일 priority 후보의 ambiguity 구조 유지

## 보조 NPC
PASS

검증:
- forge/anvil/water/grind/bench의 PLAYER_POINT와 ASSIST_POINT 비중첩
- 각 점이 walkable
- 직접 제작 공정 변화에 맞춘 호환 보조 위치 전환
- 자동 방패 제작 완료 후 STORAGE_DELIVERY_POINT 도착
- 전달 후 창고 앞에서 벗어나 PARKING_POINT 도착

## 빗자루 / 먼지
PASS

검증:
- 빗자루 미장착 상태에서 먼지는 resolver 후보가 아님
- 빗자루 바로 옆 먼지가 있어도 빗자루 pickup 우선
- 빗자루 장착 후 먼지 청소가 tool target으로 활성화
- 기존 진상 퇴치/꼬마 보호 회귀 테스트 유지

## 창고 / 진열
PASS

검증:
- 30개 완성품 목록 생성
- PageDown/ArrowDown으로 마지막 항목 접근
- 마지막 항목 진열 가능
- 진열대 6칸과 창고 목록 독립
- 6/6 상태에서 추가 진열 실패 시 창고 아이템 보존
- 진열품 회수 후 inventory 복원
- 기존 save inventory migration 유지

## 메시지 큐
PASS

검증:
- 여러 System Notification 동시 등록
- 최대 3개 stack
- Important Event가 사소한 후속 알림 때문에 밀려나지 않음
- location fade는 notification stack과 독립
- 시간 경과 후 정상 제거

## 문 NPC 동선
PASS

검증:
- 단일 holder만 Door 구간 점유
- queue 순서 처리
- 외부 WAIT_POINT queue offset 분리
- 일반 손님 3명이 교착 없이 순차 입장
- 입장 후 reservation 해제
- 같은 시스템을 상인/꼬마/퇴장 흐름에서 사용

## Compound Counter
PASS

검증:
- counterTop / counterLeg 모두 `counter_main`
- 동일 interactionGroup 공유
- 카운터 glow 호출 시 가로/세로 양쪽 모두 stroke/fill 처리
- 카운터 상호작용 후보도 같은 group 사용

## 2층 가구
PASS

검증:
- 개인 궤짝/상자가 바닥 영역으로 이동
- 침대와 최소 시각 간격 유지
- collider 존재
- depth 값이 footprint bottom과 일치
- 2층 기존 interaction point walkable 회귀 테스트 통과

## 저장 Migration
PASS

- saveVersion 3 테스트 데이터를 V5로 migration
- display 6칸 생성
- doorReservation 기본값 생성
- 기존 Gold/고객/청결 데이터 보존
- AUTO SAVE / SAVE 1~6 독립성 유지

## Production build
PASS

`npm run build` 성공.

정적 Preview 검사:
- `index.html` → HTTP 200 / text/html
- `assets/audio/bgm/the_artisans_hearth.mp3` → HTTP 200 / audio/mpeg

BGM SHA-256:
`6ce27ec9fae11678350a54e511e5bfd804496ffbd36091cf0e94321aece7c3a4`

소스 BGM과 Production BGM이 동일했습니다.

## 브라우저 자동 캡처 환경 제한
컨테이너의 Chromium headless 프로세스가 페이지 로드 후 종료되지 않아 timeout이 발생했습니다. 따라서 이 환경에서 Chromium 기반 최종 픽셀 캡처/DevTools 콘솔 자동 검증은 완료하지 못했습니다.

대신 Node 기반 Scene 통합 테스트, 실제 Production 정적 서버 응답, 전체 시스템 회귀 테스트와 구문 검사는 통과했습니다. 실제 사용자 브라우저 QA는 Render 또는 로컬 `RUN_GAME.bat` 실행본에서 최종 감각 확인을 권장합니다.
