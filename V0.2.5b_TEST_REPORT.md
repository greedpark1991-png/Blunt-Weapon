# V0.2.5b TEST REPORT

## 최종 결과

실행:
```bash
npm test
npm run build
```

결과:
```text
✓ V0.2.5b systems/collision/pathfinding tests passed
✓ V0.2.5b UX/save/keyboard/audio tests passed
✓ V0.2.5b scene integration/early-sleep/renown tests passed
✓ V0.2.5b layout/stair/merchant/tracker tests passed
✓ V0.2.5b loft collision/landing/compact-tracker regression tests passed
✓ Production build created at dist/
```

`src/`, `scripts/`, `tests/`의 모든 `.js`/`.mjs`는 `node --check`를 통과했습니다.

## V0.2.5b 추가 테스트

### 2층 충돌
- 형 침대 footprint 차단.
- 동생 침대 footprint 차단.
- 공용 테이블 차단.
- 형/동생 개인 궤짝 차단.
- 계단 랜딩 차단.
- 2층 외곽 bounds 차단.
- 반복 이동 후에도 플레이어가 walkable floor에 남는지 확인.

### 2층 접근 포인트
- 형 침대
- 동생 침대
- 공용 테이블
- 수납장
- 창문
- 등불
- 계단

모든 `LOFT_INTERACTION_POINTS`가 충돌 영역 밖 walkable 위치인지 검사했습니다.

### 2층 pathfinding
공용 시작점에서 모든 주요 상호작용 포인트까지 경로를 생성하고, 생성된 waypoint가 전부 walkable인지 검사했습니다.

### 계단
- 2층 → 1층 안전 스폰 유지.
- 1층 계단 interaction point와 충분히 분리.
- 1층 → 2층은 `loftWorld.nearestWalkable()`로 안전 좌표 확인.
- 2층 계단 상호작용 지점과 스폰을 분리.
- 0.35초 재진입 쿨다운 유지.

### 좁은 화면 작업 안내
외부 사이드 패널을 표시하기 어려운 1366px 계열을 가정해 실제 Canvas 렌더를 실행했습니다.
캔버스 내부 우측 상단에 현재 작업 / 현재 단계 / 다음 단계 요약이 표시되는 것을 PNG로 확인했습니다.

## V0.2.5a/V0.2.5 회귀

- 1층 가구/벽 충돌.
- 형제 자동 작업 pathfinding.
- 손님 CUSTOMER_ZONE 제한.
- 상인 문 진입.
- 제작 단계 추적/목표 glow.
- CLOSED 조기 취침 / OPEN 취침 금지.
- 명성 단계 및 단골.
- 진상/빗자루.
- 다중 세이브 및 V3→V4 migration.
- 키보드 Pause/상점/옵션.
- BGM/SFX 중앙 관리.

## Canvas 실제 렌더 QA

브라우저 headless 대신 프로젝트가 실제 사용하는 Canvas 렌더 함수를 Skia Canvas로 직접 실행해 아래 결과물을 생성했습니다.

- `qa/V0.2.5b_shop.png`
- `qa/V0.2.5b_loft.png`
- `qa/V0.2.5b_compact_tracker.png`

육안 검토:
- 1층 작업 라인/계단/재료창고/ㄱ자 카운터/문이 서로 분리되어 읽힘.
- 2층 침대가 중앙 병렬 구조가 아니라 좌우 개인 영역으로 분리됨.
- 공용 테이블/러그/개인 물품으로 생활공간 구분 확인.
- compact tracker가 상단 벽 쪽에 작게 표시되어 주요 작업 바닥을 가리지 않음.

## Production HTTP

Preview server:
- `/` → HTTP 200 / `text/html`
- `/assets/audio/bgm/the_artisans_hearth.mp3` → HTTP 200 / `audio/mpeg`
- BGM 응답 크기 → 4,183,367 bytes

## BGM 무결성

원본 첨부, 소스, Production build SHA-256:
```text
6ce27ec9fae11678350a54e511e5bfd804496ffbd36091cf0e94321aece7c3a4
```
모두 동일합니다.

## 브라우저 자동화 환경 제한

시스템 Chromium은 현재 실행 환경의 DBus/zygote 문제로 headless 프로세스 종료가 안정적이지 않습니다.
이 제한을 보완하기 위해 V0.2.5b에서는 프로젝트의 실제 Canvas 렌더 함수를 별도 Canvas 엔진에서 직접 실행해 1층/2층/좁은 화면 작업 안내 PNG까지 검토했습니다.

## 알려진 범위 제한

- 마을 외부/광산/벌목/전투는 미구현.
- 2층은 수면과 생활 표현 위주이며 완전한 하우징 시스템은 아님.
