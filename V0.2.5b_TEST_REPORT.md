# V0.2.5b TEST REPORT

## 최종 자동 테스트

실행:
```bash
npm test
npm run build
```

결과:
```text
✓ V0.2.5b systems/collision/pathfinding tests passed
✓ V0.2.5b UX/save/keyboard/audio tests passed
✓ V0.2.5b scene integration/early-sleep/renown tests passed
✓ V0.2.5b layout/stair/merchant/tracker tests passed
✓ V0.2.5b spatial grammar/depth/interaction tests passed
✓ Production build created at dist/
```

`src/`, `scripts/`, `tests/`의 모든 `.js`/`.mjs`를 `node --check`로 검사했고 통과했습니다.

## 공간 문법 / 충돌 검증

### 1층 footprint
다음을 실제 solid footprint로 검사했습니다.
- 측면 계단
- 장작/석탄
- 화로
- 모루
- 담금질통
- 큰 작업대
- 숫돌
- 재료 창고
- 판매 진열대
- ㄱ자 카운터 상단 판
- ㄱ자 카운터 세로 다리

각 footprint 중심은 walkable이 아니며 각 별도 interaction point는 walkable임을 확인했습니다.

### 북쪽 열 작업 라인
- fuel/forge/anvil/water 스프라이트가 north wall seam에 붙는 Y 범위인지 확인.
- 네 설비 footprint bottom 오차가 8px 이내인지 확인.
- 벽에서 떨어진 독립 섬처럼 배치되지 않게 검사.

### 숫돌 ↔ 카운터
- 두 collider 사이에 실 통로가 남는지 수치 검사.
- grind interaction → counter interaction pathfinding 성공.
- broom radius가 grind/counter interaction radius와 겹치지 않음.

### 카운터
- counterTop / counterLeg 중심은 collision.
- 직원측 접근 좌표 → counter interaction 경로 생성 성공.
- 우측 문 ENTRY → 손님 카운터 앞 위치까지 CUSTOMER_ZONE 경로 생성 성공.

### 빗자루
실제 interaction resolver를 사용해 아래 위치에서 우선 대상 확인:
- 숫돌 위치 → `grind`
- 빗자루 위치 → `broom`
- 카운터 위치 → `counter`

따라서 숫돌/카운터 중앙에서 빗자루 E가 우선 선택되지 않습니다.

## Depth Sorting 검증
- forge drawable baseY = forge floor footprint bottom.
- grind drawable baseY = grind floor footprint bottom.
- counter-top baseY = counter top footprint bottom.
- 2층 침대/table도 LOFT_DEPTHS를 사용.
- actor의 발 Y가 object baseY보다 작으면 뒤, 크면 앞이라는 동일 정렬 계약을 사용.

## 2층 검증
- 형/동생 침대가 가로보다 세로로 긴 구조인지 확인.
- 형 침대가 좌측 벽 영역, 동생 침대가 우측 벽 영역인지 확인.
- 침대/공용 테이블/개인 궤짝/계단 collider 차단.
- 모든 LOFT interaction point가 walkable.
- 1층→2층 spawn이 stairsDown interaction에서 충분히 분리됨.
- 2층→1층 spawn 역시 1층 stair interaction에서 충분히 분리됨.

## 라벨 / 이벤트 마커 검증
- broom/sign/door/storage/counter 영구 라벨 없음.
- 상호작용 프롬프트로 대상 이름 제공.
- alert marker가 최소 12px 이상의 다중 plate와 `!`를 렌더하는지 자동 확인.

## 기존 시스템 회귀
- V3 → V4 save migration.
- 수동 SAVE 1~6 / AUTO SAVE 독립.
- ESC Pause / 키보드 UI.
- 상인 다중 구매.
- 제작 일시정지.
- BGM singleton/loop/settings.
- 형/동생 자동 제작 완료.
- Customer CUSTOMER_ZONE 제한.
- CLOSED 조기 취침 / OPEN 취침 금지.
- 명성 단계 / 단골.
- 진상 빗자루 퇴치.
- 상인 문 진입.
- 제작 stage tracker / target glow.

## 실제 Canvas Scene 시각 QA

브라우저 자동화와 별개로 프로젝트의 실제 render 함수 및 `GameScene.render()`를 Skia Canvas에서 직접 실행했습니다.

프로젝트 `qa/` 폴더:
- `V0.2.5b_scene_shop.png`
- `V0.2.5b_scene_loft.png`
- `V0.2.5b_shop_spatial.png`
- `V0.2.5b_loft_spatial.png`

육안 확인:
- 북쪽 작업 라인이 벽면과 이어져 보임.
- 벽 도구가 바닥 가구보다 높은 wall plane에 있음.
- 숫돌/카운터 사이가 시각적으로 분리됨.
- ㄱ자 카운터와 문 사이 여유가 있음.
- 재료 창고가 좌하단 바닥 오브젝트로 읽힘.
- 1층 계단이 벽면 측면 계단으로 읽힘.
- 2층 침대가 중앙 병렬 숙소 배치가 아니라 좌우 개인 영역으로 읽힘.
- 공용 테이블/러그/개인 수납이 생활공간을 분리함.
- 실제 Scene의 기사 위 alert marker가 판 형태로 명확하게 보임.
- compact objective와 target glow가 실제 Scene에서 보임.

## Production HTTP
고유 테스트 포트 4187에서 Preview server를 실행했습니다.

```text
/                                         200 text/html; charset=utf-8 727 bytes
/assets/audio/bgm/the_artisans_hearth.mp3 200 audio/mpeg 4183367 bytes
```

## BGM 무결성
첨부 원본, 소스, Production build SHA-256:
```text
6ce27ec9fae11678350a54e511e5bfd804496ffbd36091cf0e94321aece7c3a4
```
세 파일 모두 동일합니다.

## 환경상 제한
이 실행 환경의 시스템 Chromium은 DBus/zygote 문제로 headless 프로세스 종료가 안정적이지 않습니다.
따라서 완전한 브라우저 자동화 대신 Node 기반 Scene 통합 테스트 + 실제 Canvas Scene 렌더 + 정적 HTTP 테스트로 검증했습니다.

## 알려진 범위 제한
- 마을 외부/광산/벌목/전투는 이번 핫픽스 범위 밖입니다.
- 2층은 생활공간/수면 중심이며 완전한 하우징 시스템은 아닙니다.
- 장작/석탄은 현재 공간 표현 및 향후 확장 기반이며 본격 연료 경제는 아직 추가하지 않았습니다.
